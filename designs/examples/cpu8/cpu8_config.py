from __future__ import annotations

DEFAULT_PARAMS = {
    "mem_depth": 256,
}

TB_PRESETS = {
    "smoke": {"timeout": 128, "finish": 20},
    "nightly": {"timeout": 512, "finish": 64},
}

SIM_TIER = "normal"
